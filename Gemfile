source "https://rubygems.org"

# Matches GitHub Pages' "classic" build environment so local previews
# behave like production. Run: bundle install && bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins

# Required to run `jekyll serve` locally on Ruby 3.0+
gem "webrick", "~> 1.8"
